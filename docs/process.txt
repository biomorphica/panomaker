  



to do

x Change Add to Commit (onchange="verifyPano" - tests all fields, and if valid, submits data )
x Delete pano button
x Test if same pano has already been used. If so, issue alert.
verify Pano data


Import json data from maxscript to populate form data.

Logo implentation

x Image thumbnails inside html UI

export json data to server.
create aframe scene when json package is received. Populate with incoming panos.

